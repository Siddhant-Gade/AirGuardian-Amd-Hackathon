# AirGuardian AI — Python dependencies
# Install: pip install -r requirements.txt
#
# Notification : Gmail SMTP via Python built-in smtplib (no paid API needed)
# LLM          : Groq Llama 3.3-70B (free tier at console.groq.com)
# ML           : PyTorch GRU — runs on CPU, no GPU required

# --- Web framework ----------------------------------------------------------
fastapi==0.110.0
uvicorn[standard]==0.29.0

# --- ML / Data --------------------------------------------------------------
torch==2.2.1
pandas==2.2.1
numpy==1.26.4
scikit-learn>=1.4.1

# --- Database ---------------------------------------------------------------
# SQLite is used via Python built-in sqlite3 — no ORM, no extra install needed.

# --- Scheduler --------------------------------------------------------------
apscheduler==3.10.4

# --- LLM client -------------------------------------------------------------
openai==1.25.0

# --- HTTP client ------------------------------------------------------------
httpx==0.27.0

# --- Config / Env -----------------------------------------------------------
python-dotenv==1.0.1
pydantic-settings==2.2.1

# --- Testing ----------------------------------------------------------------
pytest==8.1.1
pytest-asyncio==0.23.6
